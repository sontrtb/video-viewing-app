import {windowHeightByPercentage, windowWidth} from '@app/src/ultis/layout';

const widthVideo = windowWidth;
const heightVideo = windowHeightByPercentage(0.6);

const heightVideoCollapse = 60;
const widthVideoCollapse = 80;

export {widthVideo, heightVideo, heightVideoCollapse, widthVideoCollapse};
